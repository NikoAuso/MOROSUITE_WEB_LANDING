## Summary

<!-- What does this change, and why? -->

## Related issue

<!-- e.g. Closes #123 -->

## Checklist

- [ ] `npm run check` passes (astro check + tsc)
- [ ] `npm run lint` passes
- [ ] `npm run format:check` passes (or ran `npm run format`)
- [ ] `npm test` passes
- [ ] Relevant E2E suites pass (`npm run test:e2e`, `:degraded`, `:empty`)
- [ ] Updated docs (`README.md` / `CLAUDE.md` / `src/lib/dto.ts`) if behavior or the contract changed
